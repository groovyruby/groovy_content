<h1>Listing page_types</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Slug</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @page_types.each do |page_type| %>
  <tr>
    <td><%= page_type.name %></td>
    <td><%= page_type.slug %></td>
    <td><%= link_to 'Show', [:admin, page_type] %></td>
    <td><%= link_to 'Edit', edit_admin_page_type_path(page_type) %></td>
    <td><%= link_to 'Destroy', [:admin, page_type], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Page type', new_admin_page_type_path %>
